import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  textInput: {
    fontSize: 18,
    margin: 10,
    padding: 8,
    paddingBottom: 7,
    borderColor: "#dddddd",
    borderWidth: 1,
  },
})
